### REPLANNING CONTEXT
<goal>
{{ goal }}
</goal>

<current_plan_status>
{% for step in steps %}
- [{{ step.status }}] {{ step.id }}: {{ step.description }}
{% endfor %}
</current_plan_status>

<failure_reason_or_feedback>
{{ feedback }}
</failure_reason_or_feedback>

<available_artifacts>
{{ available_artifacts | tojson(indent=2) }}
</available_artifacts>

---

### INSTRUCTIONS
1. Analyze why the previous plan failed or needs adjustment based on the feedback and available artifacts.
2. Re-evaluate the dependency graph. Some completed steps might need to be re-run, or new steps inserted.
3. Formulate an updated strategy in your `reasoning` field.
4. Output the full updated plan, including both completed and new/updated steps, ensuring the `step_id`s remain consistent where possible.

Generate the updated JSON plan following the standard schema (including `reasoning` and `steps`). Return ONLY the JSON object.
