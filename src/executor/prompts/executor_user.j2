### STEP EXECUTION CONTEXT
<current_step>
{{ step_definition | tojson(indent=2) }}
</current_step>

<available_artifacts>
{{ available_artifacts | tojson(indent=2) }}
</available_artifacts>

<tools_available>
{% for tool in tool_list %}
- {{ tool }}
{% endfor %}
</tools_available>

<critical_tool_parameters>
IMPORTANT: Use these EXACT parameter formats:
- document_generator: {"title": "string", "sections": [{"heading": "string", "content": "string"}]}
- file_writer: {"filename": "string", "content": "string"}
- calculator: {"expression": "string"}
- web_search: {"query": "string"}
</critical_tool_parameters>

{% if user_preferences %}
<user_preferences>
{{ user_preferences | tojson(indent=2) }}
</user_preferences>
{% endif %}

{% if context %}
<task_context_and_history>
{{ context | tojson(indent=2) }}
</task_context_and_history>
{% endif %}

---

### INSTRUCTIONS
1. Perform the tactical analysis required by your Operational Protocol.
2. Formulate the optimal tool call based on the available artifacts and step description.
3. Ensure your `reasoning` explicitly mentions why you chose specific parameter values.
4. If the step requires data from a specific artifact, verify the artifact ID exists in the context above.

Generate the execution JSON now. Return ONLY the JSON object.
