apiVersion: v1
kind: Service
metadata:
  name: stateful-agent-service
spec:
  selector:
    app: stateful-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
